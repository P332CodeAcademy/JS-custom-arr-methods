const numbers = ["salam", "necesen"];

let num = numbers.find((element, idx, array) => {
    // console.log(idx)
    console.log(element)
    // console.log(array)
    // return element == "salam";
});
// console.log(num);

function customFind(value, index, array, itirationArr) {

    for (let i = 0; i < itirationArr.length; i++) {


    }
}